notation: goals
spec_version: "0.1"
methodology_version: "3.4.0"

# Goals tree — pure projection over standalone GOAL elements.
# Spec: notations/views/diagrams/04-goals.md (v2.0). No element data is authored inline.
# Each GOAL-… lives in canon/elements/01_motivation/goals/<GOAL-…>.yaml with
# its own notation:goal envelope, admission record, lifecycle, and parent link.

id: GOALS-FILL-ME-1
name: "FILL-ME goals tree"
description: "FILL-ME — what this tree covers"
period: "FILL-ME"
generated_at: "YYYY-MM-DD"
author: "FILL-ME"

view_config:
  scope:
    root_goal: null                    # GOAL-… to anchor as tree root; null = show all roots
    period: null                       # optional string filter on goal period metadata
    type_filter: null                  # list of type names to include; null = all types
    valid_at: null                     # ISO 8601 date; null = show all regardless of lifecycle
  goal_types:                          # display vocabulary — configures level labels
    - { name: "Strategy",       level: 0 }
    - { name: "Strategic Goal", level: 1 }
    - { name: "Project Goal",   level: 2 }
  display:
    depth: null                        # integer; null = unlimited depth
    collapsed: []                      # GOAL-… IDs of collapsed nodes at load time
