# `codex/internal/`

Policies and internal standards the organisation issues to itself — not foldered by country. `POLICY` / `INTERNAL_STANDARD` artefacts, each carrying `issuing_authority`, `effective_date`, and `applies_to`. One `<ID>.yaml` per artefact. See `notations/14-codex.md` for the frontmatter contract.
