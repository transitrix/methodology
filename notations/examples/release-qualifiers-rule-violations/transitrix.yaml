# Worked example — notations/examples/release-qualifiers-rule-violations/README.md.
# Its own catalogue root (MANIFEST.md §4), a sibling of
# notations/examples/release-qualifiers/ rather than nested inside it — no
# manifest may enclose another, which is also why this fixture moved out
# from under its parent folder.
transitrix: 1
methodology_version: "5.1.0"
notations: [requirement, assertion, verification]
zones: [canon]
coverage_profile: core
