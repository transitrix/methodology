notation: dgca
spec_version: "0.1"
methodology_version: "4.2.0"

id: DGCA-STRAT-1
name: "Strategy 2026 — DGCA chain"
description: "Driver → Goal → Change → Action decomposition for the 2026 plan."
period: "2026"
generated_at: "2026-05-26"
author: Transitrix

# Full-tier projection form example. Element data lives in elements/ alongside
# this file (post-promotion). The renderer derives the chain by traversing
# inline cross-reference fields (goal.factors, change.goals, action.changes)
# on those element files — see notations/views/diagrams/02-dgca.md §"Source of truth".
# For a self-contained inline example, see startup.dgca.transitrix.yaml.

view_config:
  goals:
    filter: all             # include every active GOAL from the companion elements/
  factors:
    surface: derived        # derive DRIVER set from goal.factors links
  changes:
    surface: derived        # derive CHANGE set from change.goals links
  actions:
    surface: derived        # derive ACTION set from action.changes links
  layers:
    drivers: on
    goals: on
    changes: on
    actions: on
