# Worked example — notations/examples/verification-reverse-trace-gaps/README.md.
# Its own catalogue root (MANIFEST.md §4), a sibling of
# notations/examples/verification/ rather than nested inside it — no
# manifest may enclose another, which is also why this fixture moved out
# from under its parent folder.
transitrix: 1
methodology_version: "3.6.0"
notations: [verification, requirement]
zones: [canon]
coverage_profile: core
